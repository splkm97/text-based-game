# CONCEPTS

## 진엔딩

한 인물에게 허락된 유일한 최종 결말 상태로, `prototype/remains.json`의 `narrativeRules.trueEnding`이 인물 4명(dusik, ru, banjang, taesan) 각 1개와 한 회차 동시 성립 불가(배타성)를 규정한다. 일반적인 분기 엔딩·배드 엔딩과 구별되며, 도달 경로를 뜻하는 아크와도 다른 개념이다.

*Avoid:* 트루엔딩, 참엔딩, 히든 엔딩

## 배치

`reclaim`에서 두 이야기 체인의 순서를 정하는 규칙 파라미터다(`ru_first` / `dusik_first`). 두 배치는 같은 흐름과 같은 콘텐츠를 공유하고 guard와 간선만 바꾼다. 캐논은 보류 상태이며, 제품 화면에 노출하지 않고 `RunState.placement`로 저장된다. 열거 테스트가 두 배치를 모두 돌려 배타 불변식을 검사한다.

*Avoid:* 순서, 루트 순서, 난이도 모드

## 상실 신호

한 갈래가 닫히거나 재료를 놓쳤을 때 화면 문면으로 그 사실을 밝히는 표시다. `reclaim`에서는 재등장 기회 소모, 군 경로 잠김, 좌표 미수신, 무대 미개방이 모두 문면으로 드러난다 — 조용한 잠금은 금지다.

*Avoid:* 잠금 안내, 실패 표시
