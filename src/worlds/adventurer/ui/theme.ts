// Palette values the UI needs as literals: an SVG `fill` prop cannot read a CSS custom property.
// Mirrors `src/styles/theme.css`. This is the only place a hex color appears under `src/ui`.

export const SLATE = "#5f574f";
